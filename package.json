{
  "name": "medical-store-system",
  "version": "1.0.0",
  "description": "Medical Store Management Full-Stack System",
  "private": true,
  "main": "backend/src/server.js",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "build:all": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build",
    "start": "node backend/src/server.js"
  }
}
